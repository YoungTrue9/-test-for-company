## Notes

The layout uses the Lato font from Google Fonts. Course images are stored in `public/images`.

Due to Figma export limitations, some font settings and CSS properties could not be extracted precisely. They can be adjusted if more accurate values are required.
